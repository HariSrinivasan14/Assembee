<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.assembee">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_transparent"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_transparent"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assembee">
        <activity android:name=".ProjectDetails"></activity>
        <activity
            android:name=".Add_Post"
            android:label="@string/title_activity_add__post"
            android:theme="@style/Theme.Assembee.NoActionBar"></activity>
        <activity
            android:name=".newPosting"
            android:label="@string/title_activity_new_posting"
            android:theme="@style/Theme.Assembee.NoActionBar"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Assembee.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>